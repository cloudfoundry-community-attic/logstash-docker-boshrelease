---
meta:
  environment: NAME
  dns_root: microbosh
  security_groups:
    - logstash_docker
  stemcell:
    name: STEMCELL

director_uuid: PLACEHOLDER-DIRECTOR-UUID

releases:
  - name: logstash-docker
    version: latest
