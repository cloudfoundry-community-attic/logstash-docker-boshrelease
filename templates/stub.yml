---
meta:
  environment: NAME
  dns_root: microbosh
  security_groups:
    - logstash_docker
  stemcell:
    name: STEMCELL

director_uuid: PLACEHOLDER-DIRECTOR-UUID

releases:
  - name: docker
    version: latest
  - name: logstash-docker
    version: latest
